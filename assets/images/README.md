# WorkTaskMe brand assets

- `worktaskme-icon.svg` — app icon (Indigo plate + W/arrow/check)
- `worktaskme-logo.svg` — full lockup on transparent/white
- `worktaskme-icon.png` — raster master for favicons / previews

Web serves copies from `web/assets/images/`.
Mobile uses `mobile/assets/images/` (+ `flutter_launcher_icons` → phone home-screen icon).
